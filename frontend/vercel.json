{
    "version": 2,
    "builds": [
      {"src": "/frontend", "use": "@vercel/static","config": { "includeFiles": ["**"] }}
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/frontend/$1"
      }
    ]
  }
